# Capstone 2.0 — Build Your Own Language

A teaching compiler project in C++ where students design and implement
their own dialect of **TIPS (Ten-Instruction Pascal Subset)**.

**Features**
- 🧩 Lexer (`rules.l`) — tokenize source code  
- 🌲 Parser (`parser.cpp`) — recursive-descent grammar  
- 🧱 AST (`ast.h`) — program structure  
- 🚀 Interpreter (`driver.cpp`) — run your code  
- 🧰 Debug tools & tests — full pipeline from lex → parse → interpret  

> “Every programming language started as someone’s project — now it’s your turn.”

**Technologies:** C++, Flex, Bash  
**Parts 1–4:** Hello World → Variables → Expressions → Custom Logic
